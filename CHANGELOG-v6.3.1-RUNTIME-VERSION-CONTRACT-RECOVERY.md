# AGROS ST2 v6.3.1 — Runtime Version Contract Recovery

- Restores the original `versiyon.js` object API used by `bot.js`.
- Restores `kisaOzet()`, `telegramOzet()` and `detayliOzet()` runtime functions.
- Adds a regression test that fails if the version module is reduced to a string export again.
- Preserves all v6.3.0 live universe, entry funnel and explainability changes.
- Does not change Renko pusu, Trade Engine, stop, BE or exit behavior.
