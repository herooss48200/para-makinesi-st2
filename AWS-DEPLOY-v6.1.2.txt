AGROS ST2 v6.1.2 AWS DEPLOY

Yerelde ZIP içeriğini ST2 reposuna çıkarın, sonra:

git status --short
git add .
git commit -m "fix(st2): v6.1.2 global runtime consistency and DNA context recovery"
git push origin main

AWS:
cd ~/apps/para-makinesi-st2
git status --short
git pull origin main
npm test
pm2 restart agros-st2 --update-env
pm2 save
pm2 status
pm2 logs agros-st2 --lines 200 --nostream

Kontrol:
grep -nE "6\\.1\\.2|GLOBAL HISTORICAL RUNTIME|Kanonik fark|PATTERN_AGGREGATE|Telegram timeout|ERROR|Unhandled|uncaught" logs-st2/agros-st2-out.log logs-st2/agros-st2-error.log | tail -200
