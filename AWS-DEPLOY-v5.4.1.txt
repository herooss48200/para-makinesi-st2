AGROS ST1 v5.4.1 AWS DEPLOY

1) Mevcut canlı data yedeği:
cd ~/apps/para-makinesi-binance
mkdir -p ~/backups/agros-st1-v541
cp -a data ~/backups/agros-st1-v541/data-$(date +%Y%m%d-%H%M%S)

2) Kod paketini mevcut repo üzerine çıkarın. data/ ve .env dosyalarını değiştirmeyin.

3) Bağımlılık ve doğrulama:
npm ci
npm run verify:st1
npm run version:fingerprint

4) PM2 yeniden başlatma:
pm2 restart para-makinesi --update-env
pm2 status
pm2 logs para-makinesi --lines 120

Beklenen log:
🧾 [ST1 FINAL CERTIFICATION] Telegram | Skor ... | ...

Beklenen Telegram başlığı:
🧾 ST1 FİNAL BİLİMSEL DENETİM — v5.4.1
