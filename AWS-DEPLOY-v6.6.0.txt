AGROS ST2 v6.6.0 AWS DEPLOY

1) git pull origin main
2) npm run verify:v660
3) pm2 restart agros-st2 --update-env
4) pm2 save
5) pm2 logs agros-st2 --lines 120

Expected runtime version:
6.6.0-LEARNED-TAKEOVER-EXPLAINABLE-PROTECTION

Post-deploy checks:
- Existing open positions keep their previous assignment.
- New positions show Takeover %X.XX and Trail X.XXT.
- Protection stages show K0/K1/K2/K3 without ambiguous "Koruma bekliyor" text.
